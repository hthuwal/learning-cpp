#include<iostream>//importing a header file

using namespace std; //using all the standard things in c++(importing a library)

int main()
{
	cout << "Hello World!" << endl;//cout -> output stream object used in conjuction with << which is called stream injection operator//endl->go to the next line or just end of the line
	return 0;// when program returns 0 implies everything above return ran fine that is main ran fine
}
